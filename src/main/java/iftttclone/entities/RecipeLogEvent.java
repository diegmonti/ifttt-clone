package iftttclone.entities;

/**
 * This enumeration represents the possible events that can happen to a recipe.
 */
public enum RecipeLogEvent {
	CREATED, CHECKED, TRIGGERED, FAILED, UPDATED, ACTIVATED, DEACTIVATED
}
